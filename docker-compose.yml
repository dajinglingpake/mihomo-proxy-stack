services:
  mihomo:
    image: metacubex/mihomo:latest
    container_name: mihomo
    restart: unless-stopped
    network_mode: host
    entrypoint:
      - /bin/sh
      - /start-mihomo.sh
    volumes:
      - ./config:/root/.config/mihomo
      - ./scripts/start-mihomo.sh:/start-mihomo.sh:ro

  metacubexd:
    image: nginx:alpine
    container_name: metacubexd
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./ui-official/metacubexd-gh-pages:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  proxy-portal:
    image: nginx:alpine
    container_name: proxy-portal
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./portal:/usr/share/nginx/html:ro
      - ./portal-nginx.conf:/etc/nginx/conf.d/default.conf:ro

  sub-store:
    image: xream/sub-store:latest
    container_name: sub-store
    restart: unless-stopped
    ports:
      - "3002:3001"
    environment:
      TIME_ZONE: Asia/Shanghai
      SUB_STORE_FRONTEND_BACKEND_PATH: /
    volumes:
      - ./sub-store-data:/opt/app/data

  mihomo-sync:
    build:
      context: .
      dockerfile: Dockerfile.mihomo-sync
    container_name: mihomo-sync
    restart: unless-stopped
    network_mode: host
    working_dir: /app
    command:
      - python
      - /app/scripts/auto_sync.py
      - --loop
    volumes:
      - ./:/app
